# Connex Windows Update Feed

This folder is the publish directory for the public Windows update feed.

Render Static Site should publish this folder directly.

Expected files:

- `latest.yml`
- `Connex-Setup-<version>.exe`
- `Connex-Setup-<version>.exe.blockmap`
